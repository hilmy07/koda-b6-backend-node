version: "3.8"

services:
  backend:
    image: backend-js
    ports:
      - "8000:8000"
    environment:
      REDIS_HOST: redis
      REDIS_PORT: 6379
    networks:
      - shared-net

networks:
  shared-net:
    external: true
